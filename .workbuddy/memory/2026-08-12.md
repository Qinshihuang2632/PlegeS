# 2026-08-12 工作日志

- 完成「化了个学」QA 证据审计：对 `qa-evidence/`、`qa-evidence/prod/`、`qa-evidence/_stale/` 三个文件夹共 159 张 PNG + 11 份非图片文件进行全量 MD5 比对、关键帧目视抽样与 JSON 结构核对，生成 `qa-evidence/qa-audit-report.md`。核心结论：_stale/ 整批可作废；R15/R16 同帧为预期缺陷证据；dom-evidence.json 与 shots.log、qa-report.md 与 html 为真重复；跨文件夹无字节级重复图像。
