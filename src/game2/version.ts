/*
 * 英了个语 · 独立版本号 (src/game2/version.ts)
 * =========================================
 * 第二款游戏「英了个语」自 v1.0.0 独立演进, 不继承「化了个学」的版本号。
 * 界面版本号必须跟随发布更新。
 */
export const YLGY_VERSION = "v1.6.3";
// v1.6.3: AI 检测可靠性修复 — ①前端检测请求加 20s 兜底超时(原无超时, 请求挂死时「AI 检测中…」永不消失、无任何反馈);
//         ②AI 不可用时不再回退「词库判定」锁定/扣血(真实词库词如 dream 曾被静默锁定为已完成, 导致固定提示在交叉格拼出 mhoot 类非词死路), 改为仅提示「非本局答案,建议更换」, 不锁词不扣血
// v1.6.2: 新增草稿机制 — 草稿模式下输入的字母只写入格子角落(小字), 不触发判定/扣血/填写计数;
//         可随时改写草稿, 关闭草稿模式后正式填写触发检测; 擦除同时清除草稿
// v1.6.1: 手机端横向自适应(难度行/昵称行分离, 昵称行窄屏自动换行, 全站防横向滚动兜底);
//         局内屏幕键盘改为 QWERTY 真实布局(三行+退格); 规则提示文案同步 AI 检测;
//         未参与排行榜的游玩结算上报后台(游玩记录)
// v1.6.0: 引入 AI(DeepSeek)单词检测, 修复多解法死锁 —— 填满词改为与生成时参考答案比对:
//         一致 → 变绿锁定; 不同 → AI 判断是否真实单词: 合法则提示释义且不锁定(可删改换答案),
//         非法则扣血红 2 秒; AI 不可用时回退词库判定; 后端 /ylgy/api/ai(密钥 DEEPSEEK_API_KEY 环境变量)
// v1.5.4: 填满即检测重做 —— 已判错的词修改字母再填满会重新检测, 错误继续扣血(每次填满都判);
//         错误词格子闪红 2 秒后恢复无色; 正确的词固定格子禁止改动并常绿至对局结束
// v1.5.1: 排行榜提交需开局一次性会话令牌(防刷榜); 控制台调试接口仅开发环境注册
