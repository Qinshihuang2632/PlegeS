/*
 * 错了个字 · 独立版本号 (src/game3/version.ts)
 * =========================================
 * 第三款游戏「错了个字」自 v1.0.0 独立演进, 不继承其他游戏/平台版本号。
 * 界面版本号必须跟随发布更新。
 */
export const CLGZ_VERSION = "v1.1.4";
// v1.1.4: AI 手写识别成为唯一判定 —— 删除字形重合度(像素比对)算法与「太潦草了」提示(含 HandwritingPad 判定逻辑与 handwriting.ts); OCR 不可用时不再回退本地判定, 改为提示稍后再试
// v1.1.3: 手写识别链路修复 — ①后端响应字段改读 TextDetections(原 TextItems 不存在, 手写识别结果一直丢失、实际全程在用印刷体兜底); ②返回字段对齐前端契约 text→recognized(原局内永远提示「难以辨认」); 取消印刷体二次回退(为空交给前端多尺寸变体/像素判定)
// v1.1.2: 手机端顶栏布局修复 —— 标题独占一行不再被挤压成竖排, 昵称/排行参与开关移到下方独立行(与化了个学同构)
// v1.1.0: AI 手写识别(腾讯云通用手写体识别)判定接入, 解除维护重新开放 —— 写对且可辨认即得分, 识别失败提示实际写成哪个字
// v1.0.7: 排行榜提交需开局一次性会话令牌(防刷榜); 结算清理冗余计分代码, 提交昵称改为 trim 后值
