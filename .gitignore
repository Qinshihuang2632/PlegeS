# 虚拟环境
.venv/
venv/
__pycache__/
*.pyc

# IDE 配置
.vscode/
.idea/

# 环境变量
.env

# 本地开发密钥(wrangler pages dev 加载, 不得入库)
.dev.vars
.dev.vars.*

# 本地依赖与工具状态(不入库)
node_modules/
.wrangler/
.zcode/

# 构建产物(dist 由 Vite 构建生成, 部署前 npm run build)
/dist/

# 排行榜数据(数据不入库: 线上存 Cloudflare KV, 本地文件仅作迁移源)
rankings.json
