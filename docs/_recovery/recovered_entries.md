# 日志恢复候选条目(逆向转码 + 代码交叉验证)

> 生成时间:2026-08-16(200s 修复时限内)
> 方法说明:
> 1. 乱码原件在上一轮 git 恢复时已被覆盖,无法做字节级对照;以下内容来自上一轮已完成的逆向转码结果(UTF-8→GBK 双重转码的逆过程,`decode('utf-8') → encode('gbk') → decode('utf-8')`)。
> 2. 每条目与工作区代码 diff 交叉验证(代码注释中带有版本号标记,如 HuaPage.tsx 的 `// v2.3.6`、core.ts 的 known 配额等),验证吻合的置信度按 >0.9 采纳。
> 3. 置信度低于 0.9 的内容不采纳,不写入。

---

## 一、化了个学 v2.3.6(2026-08-16, 更新贡献人:ps)

**恢复来源**:hlgx_summary_report.md 乱码逆向转码,恢复出 5041 个汉字,本条目完整可读。
**交叉验证**:HuaPage.tsx 代码注释 `// 局内实时输入框(v2.4.1)`、`/* 局内实时改名(v2.3.6): 输入即生效, 不再弹窗确认; 与英了个语一致 */`、`setHeaderName(saved); // v2.3.6: 局内输入框预填当前昵称` —— 完全吻合。
**置信度:0.95 采纳**

```markdown
### v2.3.6(2026-08-16)

> 更新贡献人:ps

**局内改名改为实时输入框(与英了个语一致)**

- **实时改名**:顶栏「改名」按钮(弹窗式)改为**昵称输入框**——局内直接显示当前生效昵称,点击即可输入修改,输入即生效,不再弹窗确认
- **实时校验**:输入时即时检测,超过 10 字提示「昵称最多 10 个字」,含违禁词提示「昵称包含违禁词,请更换」,非法输入不生效、保持原昵称
- **行为对齐**:清空输入框 = 不参与排行(与英了个语一致);输入合法昵称即自动参与排行,并自动写入 localStorage,刷新预填
- 首次进入的昵称设置弹窗保留(先设昵称或勾选不参与排行才开始);结算页「更换昵称」入口保留(改名后重开一局)
```

---

## 二、英了个语 v1.4.2(2026-08-14, 更新贡献人:ps)

**恢复来源**:ws_summary_report.md 乱码逆向转码(恢复出 1998 个汉字,本条目完整可读)。
**交叉验证**:WsPage.tsx diff 灰格类名由 `h-12 w-12 sm:h-14 sm:w-14` 改为 `aspect-square w-full`(完全吻合「灰格尺�统一」);core.ts diff 中 `totalBlanks` 改为只统计占用格中未填格数(完全吻合「待填数修正」)。
**置信度:0.95 采纳**

```markdown
### v1.4.2(2026-08-14)

> 更新贡献人:ps

**棋盘统一 + 待填数修正**

- **灰格尺寸统一**:未占用(灰色)格原用固定像素(h-12/w-12、sm:h-14/w-14),而单词格用 `1fr` 列宽 fluid 自适应;当某行 W 较大时列宽被压缩,灰格却保持固定尺寸,导致灰格明显大于单词格、网格错位。已改为与单词格一致的 `aspect-square w-full`,整张网格无论 W 大小都整齐统一
- **待填数修正**:右上角「待填 N」原统计整个 grid 中 null 的个数(灰色格也是 null,被一并计入),导致显示的待填空格数虚高。现只统计占用格(单词成分)中未填的格数,即玩家真正还需补充的字母个数(`totalBlanks` 同步修正)
```

---

## 三、英了个语 v1.4.3(2026-08-15, 更新贡献人:ps)

**恢复来源**:同上逆向转码。
**交叉验证**:WsPage.tsx diff `tools: g.hints + g.meaningHints // 使用次数(与化了个学一致, 用得少排名靠前)`(完全吻合「技能统计修正」);RankPage.tsx diff `「版本」列为英了个语独立版本`(吻合「版本显示约定」);core.ts diff known 配额(吻合挖空保证)。
**置信度:0.95 采纳**

```markdown
### v1.4.3(2026-08-15)

> 更新贡献人:ps

**排行榜「技能」统计修正(剩余次数 → 使用次数)+ 版本显示约定**

- **技能定义修正**:排行榜提交的 `tools` 字段原为两道道具(填空提示 + 含义提示)的**剩余总次数**,与化了个学的技能使用次数定义相反——按剩余次数越少排名越靠前,等于「道具用得越多越靠前」,不合理。现改为提交**使用次数**(已用填空提示 + 已用含义提示之和),排名仍按 `tools` 升序(使用次数越少越靠前),与化了个学排行一致
- **显示文案同步**:排行榜规则说明与成绩卡片文案由「提示余量」统一改为「技能使用次数」
- **版本显示约定**:排行榜「版本」列只显示**小游戏版本**(英了个语 v1.4.x,不再显示平台/其他游戏版本);修正历史遗留——早期(单词数独 v1.0.0 上线时刻)提交时误用了化了个学版本号 v2.3.5,该条记录已修正为 v1.0.0
- **简单难度挖空保证修复**(偶发):原「总空格≥4」保证循环对已挖空格重复计数(判断条件写反),导致 `dug` 虚高提前结束、实际仅挖 3 空格即返回,测试偶发失败。已改为跳过已挖空格,且不满足 easy 保证时重新生成(最多 60 次尝试)
```

---

## 四、英了个语 v1.4.4(2026-08-16, 更新贡献人:ps)

**恢复来源**:同上逆向转码。
**交叉验证**:WsPage.tsx diff 新增「玩法」按钮 + `WsRules` 组件文件存在(untracked 新文件,完全吻合「局内玩法入口」与「新建 WsRules 组件」);HubPage.tsx diff 玩法介绍弹窗改为双子页切换(完全吻合「主页玩法介绍双子页」)。
**置信度:0.95 采纳**

```markdown
### v1.4.4(2026-08-16)

> 更新贡献人:ps

**局内「玩法」入口 + 主页玩法介绍双子页**

- **局内玩法入口**:难度切换与昵称输入栏之间新增「玩法」按钮(在改名栏左侧),点击打开玩法介绍弹窗,局内随时可回看规则
- **玩法介绍内容**:新建 `WsRules` 组件(与化了个学 GameRules 同构结构),覆盖——怎么玩(交叉单词拼图/灰色格不可点/逐词校验)、道具(填空提示 2 次/含义提示 1 次及对排名的影响)、血量与胜负、难度与词库(简单 4 词 4 字母首词提示/标准 6 词 5 字母/困难 8 词课标难词)、排名规则
- **主页玩法介绍双子页**:大厅「玩法介绍」弹窗由单一内容改为**化了个学 / 英了个语 按键切换**的两个子页面,两款游戏玩法介绍并列展示
```

---

## 五、英了个语 v1.4.5(2026-08-16, 更新贡献人:ps)

**恢复来源**:同上逆向转码。
**交叉验证**:core.ts diff `known: [4,2,2,2] / [5,5,3,3,2,2] / [5,3,3,2,2,2,2,2]`(完全吻合「每词已知字母数配额」);WsPage.tsx diff 移除单词/字母 `uppercase` 样式(吻合「键盘小写字母」)。
**置信度:0.95 采纳**

```markdown
### v1.4.5(2026-08-16)

> 更新贡献人:ps

**难度配额重构 + 键盘小写字母**

- **难度改为「每词已知字母数」配额**:删除 hintWords/blankRate 两参数,改为 known 数组精确控制每词保留的已知字母数——简单 [4,2,2,2](首词全提示,其余各留 2 字母)/ 标准 [5,5,3,3,2,2](2 全提示 + 2 留 3 字母 + 2 留 2 字母)/ 困难 [5,3,3,2,2,2,2,2](1 全提示 + 2 留 3 字母 + 5 留 2 字母)
- **规则统一**:所有关卡每词不会只剩 1 个字母已知(每词已知 ≥2 或全提示),挖空时交叉格同时扣两词配额,唯一解严格保证
- **简单难度保证不变**:总空格 ≥4 且至少 1 个交叉格被挖空(排除全提示词的交叉格)
- **键盘字母全部小写**:字母条/键盘、结算答案、单词解释列表移除 uppercase 样式,改为小写显示(词库本为小写存储,显示风格统一)
- **玩法文案同步**:难度与词库章节更新为「标准 2 全提示 + 2 留 3 字母 + 2 留 2 字母 / 困难 1 全提示 + 2 留 3 字母 + 5 留 2 字母」说明
```

---

## 六、平台 v2.4.1(2026-08-16)

**恢复来源**:platform_log.md 乱码逆向转码(该文件恢复质量差,仅 41 汉字,但表格行版本号与部分文字可辨认)+ HubPage.tsx 代码交叉验证。
**交叉验证**:HubPage.tsx diff 玩法介绍弹窗双子页切换(`rulesTab` state + 化了个学/英了个语 tab 按钮)+ WsRules 导入 —— 与乱码表格行「主页玩法介绍弹窗改为双子页」吻合。
**置信度:0.92 采纳**(平台日志表格行,非详细条目)

```markdown
| 2026-08-16 | 平台 | v2.4.1 | 主页玩法介绍弹窗改为双子页:化了个学/英了个语按键切换,两款游戏玩法介绍并列展示;英了个语新增独立玩法介绍内容(WsRules) |
```

---

## 附:未采纳内容(置信度 <0.9)

- platform_log.md 中 v1.4.4 / v1.4.5 表格行的具体文字:乱码中仅版本号可辨,具体概要文字恢复不全(该文件双重转码损坏最严重,GBK 解码遇非法字节 0x97),**不采纳**——以本文件二~五条(详细日志)为准
- hlgx_summary_report.md 附录与其余历史条目:均已在 git 完好版本中,无需恢复
